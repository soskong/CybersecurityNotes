#### msfconsole

1. 搜索相应模块：`search [vuln]`
2. 选择对应模块：`use [vuln]`
   模块分为Auxiliary（用来探测）和exploit（利用）
3. 设置选项

   ```
   show options
   set [option] [para]
   ```
4. 攻击：`exploit`
5. 将获得meterpreter挂起 `background`
6. 重新进入对应meterpreter `sessions [num]`
7.
